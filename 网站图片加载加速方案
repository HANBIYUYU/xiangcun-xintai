📁 第一步：项目结构准备
确保你的静态图片都放在 public/assets/ 目录下（Vite 默认约定），结构如下：

text
你的项目/
├── public/
│   ├── assets/
│   │   ├── logo.png
│   │   ├── banner.jpg
│   │   ├── icon.svg
│   │   └── ...（所有图片）
│   └── _headers          ← 待创建
├── src/
│   └── ...
├── vite.config.js        ← 待修改
└── package.json
如果你的图片散落在 src/ 下被 import 引用，也建议统一移到 public/assets/，然后用绝对路径引用（如 /assets/logo.png），这样 Vite 不会二次处理，加载效率更高。

📄 第二步：创建 _headers 文件
在 public/ 目录下创建 _headers 文件（无扩展名），内容如下：

text
# 所有图片 1 年强缓存
/assets/*.png
  Cache-Control: public, max-age=31536000, immutable
/assets/*.jpg
  Cache-Control: public, max-age=31536000, immutable
/assets/*.jpeg
  Cache-Control: public, max-age=31536000, immutable
/assets/*.webp
  Cache-Control: public, max-age=31536000, immutable
/assets/*.svg
  Cache-Control: public, max-age=31536000, immutable
/assets/*.gif
  Cache-Control: public, max-age=31536000, immutable
/assets/*.ico
  Cache-Control: public, max-age=31536000, immutable

# JS/CSS 也缓存
/*.js
  Cache-Control: public, max-age=31536000, immutable
/*.css
  Cache-Control: public, max-age=31536000, immutable

# HTML 不缓存（保证更新后立即生效）
/*.html
  Cache-Control: no-cache, must-revalidate
部署后 Cloudflare Pages 会自动读取这个文件，给匹配的路径添加响应头。

🖼️ 第三步：全局替换图片引用（统一路径）
确保所有图片都用绝对路径引用：

html
<!-- ❌ 错误：相对路径 -->
<img src="../assets/logo.png">

<!-- ✅ 正确：绝对路径（从 / 开始） -->
<img src="/assets/logo.png">
在 React/Vue 组件中：

jsx
// ✅ 正确
<img src="/assets/logo.png" alt="Logo" />

// ❌ 错误（会被 Vite 额外处理，且不利于缓存）
import logo from '@/assets/logo.png'
<img src={logo} alt="Logo" />
🏗️ 第四步：配置 Vite 构建压缩
安装插件：

bash
npm install vite-plugin-imagemin -D
修改 vite.config.js：

js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'  // 或 vue()
import viteImagemin from 'vite-plugin-imagemin'

export default defineConfig({
  plugins: [
    react(),
    viteImagemin({
      // 只处理 public 目录下的图片
      includePublic: true,
      // 静默模式（不输出日志）
      silent: false,
      // 各格式压缩配置
      gifsicle: {
        optimizationLevel: 3,
      },
      mozjpeg: {
        quality: 80,
      },
      optipng: {
        optimizationLevel: 7,
      },
      webp: {
        quality: 75,
      },
    }),
  ],
})
这样每次 npm run build 时，所有图片都会被自动压缩（不改变原图，只优化输出）。

📝 第五步：给所有 <img> 加懒加载（全局搜索替换）
如果你用的是 React
写一个封装的 LazyImage 组件：

jsx
// components/LazyImage.jsx
export default function LazyImage({ src, alt, className, ...props }) {
  return (
    <img
      src={src}
      alt={alt}
      className={className}
      loading="lazy"
      decoding="async"
      {...props}
    />
  )
}
然后全局替换所有 <img> 为 <LazyImage>。

如果你用的是 Vue
vue
<!-- components/LazyImage.vue -->
<template>
  <img :src="src" :alt="alt" loading="lazy" decoding="async" />
</template>

<script setup>
defineProps(['src', 'alt'])
</script>
如果你用的是原生 HTML
全局搜索 <img 替换为 <img loading="lazy"（注意保留原有属性）。

首屏重要图片保留 fetchpriority="high"：

html
<!-- 首屏 Banner 优先加载 -->
<img src="/assets/banner.jpg" fetchpriority="high" loading="eager" alt="Banner" />

<!-- 其他所有图片懒加载 -->
<img src="/assets/photo.jpg" loading="lazy" alt="描述" />
🚀 第六步：部署
bash
# 构建
npm run build

# 提交并部署（如果使用 Git 自动部署）
git add .
git commit -m "优化静态图片加载速度"
git push
如果 Cloudflare Pages 配置了自动部署，推送后会自动构建并上线。

✅ 第七步：验证效果
部署完成后，打开浏览器 F12 → Network 面板：

首次访问（清空缓存后）：

图片状态码应为 200，响应头有 cf-cache-status: MISS（首次未命中缓存）

但文件体积应该比原来小（说明压缩生效）

刷新页面（第二次访问）：

图片状态码应为 200（来自 Disk Cache），响应头 cf-cache-status: HIT

Cache-Control 应为 public, max-age=31536000, immutable

加载时间应接近 0ms

📊 预期效果对比
指标	优化前	优化后
图片体积	285KB	~60-80KB
首次加载	等待回源	从 CDN 边缘返回
二次加载	304 协商缓存（仍有延迟）	0ms（浏览器强缓存）
首屏加载图片数	全部加载	只加载可见区域
