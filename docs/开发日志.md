# 湘村新台 · 开发日志（按阶段记录进展与阻碍）

> 每完成一个 P 阶段提交一次 git；中途遇到的阻碍在此记录，含现象、原因与解决方式。

---

## P0 脚手架 ✅（commit `a9accc0` / `a379c76`）

- 阻碍：无。monorepo 骨架照搬 mili-edu，前后台路由可编译运行。

## P1 数据库 ✅（commit `a379c76`）

- 阻碍 1：`wrangler d1 migrations apply` 会把 `migrations/` 下所有 `.sql` 视为迁移，种子文件被当成迁移自动应用，导致 `db:seed` 重复执行时撞 UNIQUE 约束。
  - 解决：种子移出 `migrations/` 至 `apps/api/seeds/seed.sql`，`db:seed` 单独执行。
- 阻碍 2：遗留的 `wrangler dev` 进程锁定本地 D1 文件（Windows 无法删除被占用文件），且 WMI 查询被沙箱拒绝。
  - 解决：用 WMI 提权查询进程命令行定位到 `wrangler dev`，停止后删除 `.wrangler/state/v3/d1` 重置，再重跑 migrate+seed。

## P2 API 基础 ✅（commit `266127b`）

- 阻碍：本地开发环境（http://127.0.0.1）下登录 Cookie 无条件带 `Secure` 标志，浏览器不保存 → 本地登录失效。
  - 解决：`ENVIRONMENT === 'production'` 时才加 `Secure`，dev 不加；登录/登出两处同步修改，curl 冒烟验证通过。

## P3 前端框架（进行中）

- 阻碍：`apps/web` 首次 `tsc --noEmit` 报 13 处 TS2739 —— `@ant-design/icons@5.3` 的图标 props 类型缺少 `onPointerEnterCapture/onPointerLeaveCapture`，与 `@types/react@18.3` 不兼容（P0 只经 vite/esbuild 构建未做类型检查，故遗留）。
  - 解决：升级 `@ant-design/icons` 至 `^5.6.1`，全量 `tsc --noEmit` 通过。
- 产出：`apps/web/src/api/index.ts` 补齐 14 组资源 API（auth/stages/red-plays/articles/activities/submissions/suggestions/quiz/study-plans/bookings/study-results/products/orders/coupons/faq/news/stats/upload），公开接口支持 `skipAuthRedirect` 避免游客操作被强制跳登录；`AdminLayout` 菜单按 team/admin 角色动态渲染。

## P4 首页门户 ✅（commit `f2aa145`）

- 产出：后端新增 `GET /api/stats`（戏台数/红色旧址/研学人次/销售额/待审投稿），`GET /api/news`、`GET /api/red-plays` 列表实现；前端数据看板、专题短片、新闻区全部接 API，接口失败时降级显示示例数据。
- 备注：PowerShell 控制台 curl 中文显示乱码为终端编码问题，接口返回的 UTF-8 JSON 正常。

## P5 档案库 ✅（commit 待填）

- 产出：`GET /api/stages`（乡镇/文保/破损/红色旧址/关键词 + 分页）、`/meta/towns`（乡镇下拉）、`/export`（Excel 兼容 CSV，UTF-8 BOM）、`/:id`（详情）、`/:id/export`；前端档案馆列表（筛选 + 搜索 + 分页 + 导出）与单台详情页（史料/红色事迹/修缮记录 + 导出）。
- 阻碍/决策：Worker 端手写含中文字体的 PDF 成本高（CID 字体子集化复杂），单台导出采用「服务端返回打印排版 HTML → 浏览器打印/另存为 PDF」方案；如需服务端真 PDF，后续可引入 pdf-lib + 中文字体子集化。路由顺序注意：`/export` 必须注册在 `/:id` 之前，否则被参数路由吞掉。
- 验证：乡镇筛选/关键词/详情/乡镇列表/CSV(BOM)/打印 HTML 全部通过。

## P6 三维展厅 ✅（commit 待填）

- 产出：`components/Stage3D.tsx` 程序化古戏台模型（台基/戏台红柱/后墙/四坡顶/宝顶/匾额）+ OrbitControls 720° 旋转缩放 + 自适应与资源释放；`pages/Hall3D.tsx` 展厅页（介绍弹层拉取档案详情、全屏、操作提示）。
- 阻碍/优化：引入 three.js 后主包膨胀至 1.69MB —— 用 `React.lazy` 把展厅页独立分包，主包回落至 1.13MB，three 独立 555KB（gzip 142KB）。另注意 React StrictMode 双挂载下 renderer 清理（dispose + removeChild），防止画布重复。

## P7 文化馆 ✅（commit `8dce27f`）

- 产出：`GET /api/articles`、`GET /api/activities`（status 过滤）列表实现；文化馆三子页：红色戏曲（封面卡片 + iframe 播放弹层）、互动阅读（列表 + 全文弹层）、活动预告（类型/状态标签 + 时间地点）。
- 备注：视频 iframe 白名单沿用 bilibili/腾讯/YouTube，种子中的 bvid 为占位，待正式视频接入。

## P8 共创平台 ✅（commit 待填）

- 产出：投稿（游客 POST → 待审核 → team 审核 PUT /:id/review → 前台 status=已通过 公开展示）、建言（游客 POST 自动分类 → 列表 → 状态流转 → CSV 导出）、答题（GET /random 随机 10 题、POST /submit 判分，通关 ≥80% 发放立减 10 元优惠券，source=答题）。前端共创页三 Tab：投稿+已通过展示、建言+动态、答题闯关+领券。
- 决策：通关线定为 80%（10 题对 8 题），比全对更易发放券、提升互动转化；优惠券码 XC+时间戳36进制+随机数，UNIQUE 冲突概率极低。
- 阻碍（备注）：PowerShell 命令行传中文 JSON 会乱码（编码问题），冒烟测试改用 \u 转义；验证后清理了测试数据行。
- 验证：未登录审核 401、team 审核生效、答题全对发券、≥80% 判定均通过。

---
