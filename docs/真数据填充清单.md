# 湘村新台 · 假数据 / 占位文案清单 与 真数据填充计划

> 目的：盘点当前所有「示例/占位/拟名」数据与文案，明确上线前需要替换为的真数据（台账、素材、地图、3D 模型等）及填充方式。
> 数据源说明：种子数据见 `apps/api/seeds/seed.sql`；前台降级文案见 `apps/web/src/pages|sections`。

---

## 一、数据库种子数据（seeds/seed.sql）

| 表 | 当前假数据 / 占位内容 | 待填充真数据 | 填充方式 |
|---|---|---|---|
| `admins` | 演示账号 `team` / `admin`，密码 `xiangcun2026` | 正式账号与强密码 | 上线前改密（后台/SQL） |
| `stages`（15 条完整示例） | 名称/乡镇/史料/红色事迹为**拟真示例**（如「桂阳文庙古戏台」，非官方台账口径） | 110 座真实戏台：名称、乡镇、文保等级、破损程度、始建年代、形制、建筑史料、红色事迹、修缮记录 | 官方台账 Excel/CSV → 后台「戏台档案」页逐条录入或批量导入（字段已就绪） |
| `stages`（95 条占位） | 名称「XX·第N号古戏台（占位）」；年代/形制「待考（占位）」；史料「占位数据：台账待补录」；红色事迹多数为空 | 同上，逐条替换或删除占位行 | 同上 |
| `stages.cover_url` | `placehold.co` 主题色占位图 | 真实戏台照片（现状/细节/匾额等） | R2 上传或外链图床 URL（后台已支持外链录入） |
| `stages.lng/lat` | 程序生成的近似坐标（112.60~112.88, 25.62~25.96） | 真实经纬度（供地图模块与实地导览） | 测绘/GPS 采集后更新 |
| `stages.audio_url` | 全部为空 | 村民口述音频（每座重点戏台 1 段+） | 录音转存 R2/外链，后台录入 |
| `red_plays` | `iframe_src = bvid=PLACEHOLDER1~6`；封面占位 | 真实演出视频嵌入地址（bilibili / 腾讯 / YouTube 白名单）+ 真实封面 | 后台「红色戏曲」页替换 |
| `articles` | 正文「（正文待补录）」；来源拟名（「桂阳发布」等） | 真实公众号推文图文与出处 | 后台「互动阅读」页补录 |
| `activities` | 2026-08 ~ 10 示例档期 | 真实活动排期（需与线下运营同步更新） | 后台「活动预告」页维护 |
| `submissions` | 3 条示例（李大叔/张阿姨/王先生） | 真实用户投稿（后台审核后公开展示） | 由游客提交产生 |
| `suggestions` | 3 条示例建言 | 真实用户建言 | 由游客提交产生 |
| `quiz_questions` | 40 题为 AI 编写示例（含答案解析） | 依据真实台账校订、扩充（≥40 题，标注来源） | 后台「题库」页新增/修改 |
| `study_plans` | 行程/课件/师资为示例文本 | 真实行程单、课件链接、师资名单 | 后台录入（当前无独立管理页，可并入后台或直接 SQL） |
| `study_results` | 4 条示例（郴州一中/湖南大学等为拟名） | 真实研学成果实拍与心得体会 | 后台录入（同上） |
| `products` | 8 件示例商品、示例价格/库存、占位封面 | 真实商品图、价格、库存、农户代销信息 | 后台「商品」页维护 |
| `orders` | 2 条示例订单（假联系人） | 真实订单（由用户下单产生） | 用户操作产生 |
| `coupons` | 4 条示例券（假手机号 138\*\*\*\*0031 等） | 真实发放记录（答题/投稿/活动） | 用户操作产生 |
| `faq_entries` | 14 条人工编写 QA | 依据官方资料校订（后续可接大模型语义问答） | 后台「问答库」页维护 |
| `news` | 5 条示例新闻（标题/日期为拟） | 真实校地合作新闻稿 | 后台录入（当前无独立管理页） |

## 二、前台占位文案 / 降级数据（页面内）

| 位置 | 当前内容 | 真数据来源 / 说明 |
|---|---|---|
| 首页·专题短片（`Home.tsx`） | 无 API 数据时显示 3 条静态占位卡 | 后台「红色戏曲」页新增 `category=演出视频` 条目即自动展示（无需单独管理页） |
| 首页·最新动态（`Home.tsx`） | 无 API 数据时显示 3 条静态占位新闻 | `news` 表数据 |
| 首页·数据看板（`StatsSection`） | 接口失败时降级显示示例值（110 座 / --） | `/api/stats` 实时统计（随真实数据自动变化） |
| 档案馆·详情（`ArchiveDetail`） | 「该戏台档案正在补录中」空态 | 台账补录完成后自然消失 |
| 文化馆·戏曲弹层 | 「视频源待接入（白名单：bilibili/腾讯/YouTube）」 | 替换 `iframe_src` 后生效 |
| 文化馆·阅读弹层 | 「（正文待补录）」 | 补录 `articles.content` |
| AI 台小湘 | 固定问答库关键词匹配（含兜底引导语） | 可后续接入大模型扩展语义问答（尽力项，非必须） |
| 各列表空态文案（暂无戏曲/商品/成果等） | 正常业务空态 | 数据填充后消失 |

## 三、素材资源清单

| 类型 | 现状 | 需要填充 | 备注 |
|---|---|---|---|
| 图片（全站 cover_url） | `placehold.co` 主题色占位图（600×400） | 真实照片：戏台实景/文物细节、活动、商品、新闻配图、品牌 Logo | R2 未开通 → 先用外链图床 URL（后台已支持录入）；开通后走 R2 上传 |
| 视频 | `bvid=PLACEHOLDER` 占位 | 正式红色戏曲/演出视频（bilibili/腾讯/YouTube） | 嵌入白名单已配置 |
| 音频 | 无 | 村民口述音频 | 存 R2/外链，写入 `stages.audio_url` |
| 3D 模型 | `Stage3D.tsx` 程序化几何占位（台基+红柱+屋顶+宝顶，非真实测绘） | 2~3 座重点戏台真实 glb 轻量化模型（激光扫描/手工建模） | 替换或新增模型加载逻辑（plan P6 已预留） |
| 地图 | **未实现**（仅 `stages.lng/lat` 字段预留） | 古戏台分布地图（高德/Leaflet），点击点位弹出戏台简介 | 需求文档建议项：可与档案库结合或独立模块 |
| 台账文件 | 无 | 110 座戏台官方 Excel/CSV 台账（含坐标） | 用于 `stages` 批量导入 |

## 四、配置类待办

| 项 | 现状 | 待办 |
|---|---|---|
| `JWT_SECRET` | dev 明文变量；生产已设独立值 | 生产环境用 Cloudflare secrets 覆盖，勿入库 |
| R2 / KV | `wrangler.toml` 注释占位（R2 需支付方式） | 开通后启用 `/api/upload` 与预签名 URL |
| CORS 白名单 | `localhost:5173` + `xiangcun-xintai.pages.dev` | 绑定正式域名后补充 |
| 部署 | 未部署 | P13：`wrangler deploy` + Pages 上线 |

---

*注：涉及「后台录入」但当前无独立管理页的表（study_plans / study_results / news），已具备 API 与种子数据，可在 P13 或后续补管理入口，或直接由开发人员 SQL 维护。*
