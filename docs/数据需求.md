# 湘村新台 · 假数据 / 占位文案清单 与 真数据填充计划

> 目的：盘点当前所有「示例/占位/拟名」数据与文案，明确上线前需要替换为的真数据（台账、素材、地图、3D 模型等）及填充方式。
> 数据源说明：种子数据见 `apps/api/seeds/seed.sql`；前台降级文案见 `apps/web/src/pages|sections`。
> **素材存储决策（2026-08-21）**：官方素材默认**打包进 Pages**（`apps/web/public/assets/`，单文件 ≤25MiB），R2 仅用于用户投稿/大文件；地图走 GeoJSON 文件导入、3D 走 OBJ 文件导入。详见 `docs/需求与计划.md` §7/§7.5。

---

## 一、数据库种子数据（seeds/seed.sql + seeds/real_stages.sql）

- **admins**：演示账号 `team`/`admin`（密码 `xiangcun2026`）→ 正式账号与强密码（上线前改密）
- **stages（✅ 已真实化）**：原 15 示例 + 95 占位 → 已由 `ancient_stages.geojson` 导入 **42 座真实文保戏台**（本地+远程 D1），占位全部清除；待补：**破损程度**（现为默认"较好"）、红色事迹、口述音频
- **stages.cover_url（✅）**：`placehold.co` 占位 → **42 张真实戏台实拍图**（`stage-images/`，随包部署），另有 12 张 10-19MB 大图待压缩
- **stages.lng/lat（✅）**：程序近似坐标 → **真实十进制坐标**（Longitude_DD/Latitude_DD，与地图一致）
- **stages.audio_url**：全部为空 → 村民口述音频（≤25MiB 打包，长音频走 R2/外链）
- **red_plays**：`bvid=PLACEHOLDER1~6` → 真实 B 站视频（裸 iframe 嵌入）+ 真实封面
- **articles**：正文「（正文待补录）」→ 真实公众号推文图文与出处
- **activities**：2026-08~10 示例档期 → 真实活动排期（与线下运营同步）
- **submissions**：3 条示例 → 真实用户投稿（审核后公开展示，由游客产生）
- **suggestions**：3 条示例建言 → 真实用户建言（由游客产生）
- **quiz_questions**：40 题 AI 编写示例 → 按真实台账校订、扩充（≥40 题）
- **study_plans**：行程/课件/师资示例文本 → 真实行程单、课件链接、师资名单
- **study_results**：4 条拟名示例 → 真实研学成果实拍与心得
- **products**：8 件示例商品 → 真实商品图、价格、库存、农户代销信息
- **orders / coupons**：示例订单与假手机号券 → 由用户操作产生的真实记录
- **faq_entries**：14 条人工 QA → 依据官方资料校订（后续可接大模型）
- **news**：5 条示例新闻 → 真实校地合作新闻稿

## 二、前台占位文案 / 降级数据（页面内）

- **首页·专题短片**（`Home.tsx`）：无数据时显示 3 条静态占位卡 → 后台「红色戏曲」新增 `category=演出视频` 即自动展示
- **首页·最新动态**（`Home.tsx`）：无数据时 3 条静态占位新闻 → `news` 表数据
- **首页·数据看板**（`StatsSection`）：接口失败降级示例值 → `/api/stats` 实时统计
- **档案馆·详情**（`ArchiveDetail`）：「该戏台档案正在补录中」空态 → 台账补录后消失
- **文化馆·戏曲弹层**：「视频源待接入」→ 替换 `iframe_src`（B 站裸 iframe）后生效
- **文化馆·阅读弹层**：「（正文待补录）」→ 补录 `articles.content`
- **AI 台小湘**：固定问答库关键词匹配 → 后续可选大模型语义问答
- **各列表空态**（暂无戏曲/商品/成果等）→ 正常业务空态，数据填充后消失

## 三、素材资源清单

- **图片**（全站 cover_url）：`placehold.co` 占位 → 真实戏台/活动/商品/新闻照片与品牌 Logo（打包进 Pages；R2 开通后也可走上传）
- **视频**：`bvid=PLACEHOLDER` → 正式演出视频（**上传 B 站，裸 iframe 嵌入**；后续可选 Cloudflare Stream 私有视频）
- **音频**：无 → 村民口述音频（短音频打包 / 长音频 R2 或外链）
- **3D 模型**：`Stage3D.tsx` 程序化几何占位 → **2~3 座重点戏台 OBJ 文件**（+ MTL/贴图，≤25MiB/文件，three.js OBJLoader 加载）
- **地图**：未实现（仅 `lng/lat` 预留）→ **GeoJSON 文件**（`guiyang-stages.geojson`）+ 高德/Leaflet 画点弹简介
- **台账文件**：无 → 110 座戏台官方 Excel/CSV（含坐标，用于批量导入）

## 四、配置类待办

- **JWT_SECRET**：dev 明文变量、生产已设独立值 → 生产用 Cloudflare secrets 覆盖
- **R2 / KV**：`wrangler.toml` 注释占位（R2 需支付方式）→ 开通后启用 `/api/upload`
- **CORS 白名单**：`localhost:5173` + `xiangcun-xintai.pages.dev` → 绑定正式域名后补充
- **部署**：已上线（2026-08-21）→ 后续更新执行 `pnpm deploy:api` + `pnpm deploy:web`

---

*注：study_plans / study_results / news 无独立后台管理页，当前可后台外链补充或由开发 SQL 维护，P13 后可补管理入口。*
